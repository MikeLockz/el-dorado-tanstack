version: "3.9"

services:
  promtail-sidecar:
    image: grafana/promtail:3.2.0
    container_name: promtail-sidecar
    restart: unless-stopped
    user: root
    environment:
      OBSERVABILITY_LXC_IP: ${OBSERVABILITY_LXC_IP:?Set OBSERVABILITY_LXC_IP before running promtail-sidecar}
    ports:
      - "9080:9080"
    command:
      - -config.file=/etc/promtail/promtail-config.yml
      - -config.expand-env=true
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./config/promtail-config-app.yml:/etc/promtail/promtail-config.yml:ro
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:9080/ready"]
      interval: 30s
      timeout: 5s
      retries: 5

  node-exporter-sidecar:
    image: prom/node-exporter:v1.8.1
    container_name: node-exporter-sidecar
    restart: unless-stopped
    privileged: true
    pid: host
    command:
      - --path.procfs=/host/proc
      - --path.sysfs=/host/sys
      - --collector.filesystem.ignored-mount-points=^/(sys|proc|dev|host|etc)($|/)
      - --collector.filesystem.ignored-fs-types=^(tmpfs|devtmpfs|overlay|squashfs)$
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/host:ro,rslave
    ports:
      - "9100:9100"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:9100/metrics"]
      interval: 30s
      timeout: 5s
      retries: 5

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.16.0
    container_name: postgres-exporter
    restart: unless-stopped
    environment:
      - DATA_SOURCE_NAME=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/el_dorado?sslmode=disable
    ports:
      - "9187:9187"
    depends_on:
    - postgres