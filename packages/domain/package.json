{
  "name": "@game/domain",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev:watch": "tsc -p tsconfig.json --watch --preserveWatchOutput false",
    "test": "vitest run",
    "lint": "echo 'No domain lint configured yet'"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
